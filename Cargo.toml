[package]
name = "arena_rs"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "arena_bench"
harness = false
